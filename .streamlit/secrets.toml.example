# Copy to secrets.toml for local Postgres, or paste into Streamlit Cloud > Settings > Secrets.
# Leave unset to use the local SQLite file (stonks.db).
DATABASE_URL = "postgresql://user:password@host:5432/dbname"
